# set-option -g default-shell /Users/tom.thompson/bin/fish

set -sg escape-time 0

setw -g aggressive-resize off

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
setw -g mode-keys vi

# copy paste
#set-option -g default-command "reattach-to-user-namespace -l bash"

#bind-key -t vi-copy v begin-selection
#bind-key -t vi-copy y copy-pipe "reattach-to-suer-namespace pbcopy"
#
#unbind -t vi-copy Enter
#bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"


######################
### DESIGN CHANGES ###
######################

# panes
set -g pane-border-style fg=black
set -g pane-active-border-style fg=brightred

## Status bar design
# status line
#set -g status-utf8 on
set -g status-justify left
set -g status-bg default
set -g status-fg colour12
set -g status-interval 2

# messaging
set -g message-style fg=black,bg=yellow
set -g message-command-style fg=blue,bg=black

#window mode
setw -g mode-style fg=colour0,bg=colour6

# window status
setw -g window-status-format " #F#I:#W#F "
setw -g window-status-current-format " #F#I:#W#F "
setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "

setw -g window-status-style fg=black,bg=green,reverse
setw -g window-status-current-style fg=colour11,bg=colour0,dim

# Info on left (I don't have a session display for now)
set -g status-left ''

# loud or quiet?
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

# The modes {
setw -g clock-mode-colour colour135
setw -g mode-style fg=colour154,bg=colour238,bold
# }
# The panes {
set -g pane-border-style fg=colour238,bg=colour235
set -g pane-active-border-style fg=colour51,bg=colour236
# }
# The statusbar {

set -g status-style fg=colour10,bg=colour234,dim

set -g status-position top
set -g status-left ''

set -g status-right '#[fg=colour220]#{pane_current_path} #[fg=colour219]#(cd #{pane_current_path}; git rev-parse --abbrev-ref HEAD) #[fg=colour14]#S #[fg=colour137]#H #[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 500
set -g status-left-length 20

set -g window-status-current-style fg=colour81,bg=colour238,bold
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '

setw -g window-status-style fg=colour138,bg=colour235,none
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

setw -g window-status-bell-style fg=colour255,bg=colour1,bold

# }
# The messages {
set -g message-style fg=colour232,bg=colour166,bold
# }

bind @ choose-tree 'join-pane -hs %%'
#bind @ split-window -h \; choose-tree 'join-pane -hs %%' -b 'kill-pane'
# bind > list-panes \; command-prompt -p "%%" %%
